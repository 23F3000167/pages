name: Display Email

on: [push]

jobs:
  show-email:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Show user email
        run: echo "23f3000167@ds.study.iitm.ac.in"
